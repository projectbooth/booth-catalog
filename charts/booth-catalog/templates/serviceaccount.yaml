{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "booth-catalog.serviceAccountName" . }}
  labels:
    {{- include "booth-catalog.labels" . | nindent 4 }}
{{- end }}
